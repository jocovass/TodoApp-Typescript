import React from "react";
import { State } from "../../react-app-env";

interface Props {
  todos: State;
}

const DoneTodos: React.FC<Props> = ({ todos }) => {
  return null;
};

export default DoneTodos;
